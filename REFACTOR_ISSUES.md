# GeoMine3D 重构问题记录

本文档持续记录 `codex/geomine-workbench-fastapi` 分支重构期间发现的问题、决策、处理结果与待办事项。

## 记录格式

- 状态：`待处理` / `处理中` / `已解决` / `接受风险`
- 范围：前端 / Three.js / 后端 / 数据 / 工程化
- 每条问题记录现象、影响、处理方案和验证方式。

---

## ISSUE-001：工作区存在非本次任务的未提交内容

- 状态：接受风险
- 范围：工程化
- 发现时间：2026-08-27
- 现象：创建重构分支前，工作区已经存在 `BoreholeModelLoader.ts` 的纯缩进变化，以及若干未跟踪的 Markdown、JavaScript 和 `AGENTS.md` 文件。
- 影响：若直接执行 `git add .`，可能把用户已有内容混入重构提交。
- 处理：保留所有既有文件和修改，不回滚；本分支始终使用明确路径暂存，只提交本次重构涉及的文件。
- 验证：每次提交前检查 `git diff --cached --name-status`。

## ISSUE-002：当前后端实现与目标技术栈不一致

- 状态：处理中
- 范围：后端
- 发现时间：2026-08-27
- 现象：当前业务数据主要来自 JSON/Excel 文件，后端并非目标的 FastAPI + SQLAlchemy + MySQL 持久化架构。
- 影响：前端 API、数据模型、错误格式和文件访问方式需要重新定义。
- 处理：建立新的 FastAPI 模块化单体，使用 SQLAlchemy 2.0、Alembic 与 MySQL；保留现有数据作为迁移源，不在第一阶段删除旧实现。
- 验证：API 健康检查、数据库建表、核心 CRUD 与 OpenAPI 文档可用。

## ISSUE-003：Three.js 生命周期清理不完整

- 状态：已解决
- 范围：Three.js
- 发现时间：2026-08-27
- 现象：`ModelManager.clear()` 只将对象移出场景并清空 Map，没有递归释放模型内部的 geometry、material 和 texture。
- 影响：反复进入、退出或重新加载场景时可能产生 GPU 资源泄漏。
- 处理：引入带引用计数的 `ResourceTracker`，由 `ModelManager` 在注册、移除和清空模型时统一追踪及释放 geometry、material、texture；高亮恢复时同步 dispose 临时材质。
- 验证：生产构建通过。后续仍需在真实模型环境重复装载/卸载，并确认 `renderer.info.memory.geometries/textures` 不持续增长。

## ISSUE-004：现有 SceneCanvas 承担过多编排职责

- 状态：待处理
- 范围：前端 / Three.js
- 发现时间：2026-08-27
- 现象：组件直接创建并协调大量 Manager、Tool、watcher 和生命周期逻辑。
- 影响：UI、业务状态和渲染运行时耦合，后续工作台面板联动难以维护。
- 计划：增加 `GeoEngine` 门面和类型化业务事件，Vue 组件只负责容器生命周期与命令转发。
- 验证：工作台组件不直接操作 Mesh/Material，Three.js 原生对象不进入普通 Pinia 响应式状态。

## ISSUE-005：性能数字尚无可复现基线

- 状态：待处理
- 范围：Three.js / 工程化
- 发现时间：2026-08-27
- 现象：简历描述包含 FPS 与 Draw Call 优化数字，但仓库内没有固定数据集、测试设备和前后对照记录，当前钻孔仍为普通 Mesh。
- 影响：无法验证“12 FPS 到 60 FPS”“12000+ 到约 10”等指标。
- 计划：完成实例化钻孔后建立性能基线文档，记录设备、浏览器、分辨率、模型数量、三角形数、Draw Call 和帧耗时。
- 验证：保留可重复执行的测试数据与结果截图/日志。

## ISSUE-006：新旧后端需要短期共存

- 状态：处理中
- 范围：后端 / 工程化
- 发现时间：2026-08-27
- 现象：现有 Django 文件读取服务承载着当前演示数据，而新的 FastAPI 数据库尚未完成数据迁移。
- 影响：立即删除旧后端会使现有前端失去数据来源，也不利于核对业务规则。
- 处理：在 `backend/app` 中建立 FastAPI 应用，旧 Django 目录暂时保留为迁移参照；新的 `requirements.txt`、启动方式和数据库迁移以 FastAPI 为准，完成数据迁移与前端切换后再移除旧入口。
- 验证：迁移期间两套源码互不导入；前端切换后所有 `/api/v1` 请求均由 FastAPI 提供。

## ISSUE-007：系统 Python 命令不可用且运行时尚未安装后端依赖

- 状态：已解决
- 范围：工程化 / 后端
- 发现时间：2026-08-27
- 现象：系统 `python` 指向 Windows 商店占位程序；工作区自带 Python 可以执行语法编译，但尚未安装 FastAPI、SQLAlchemy 等项目依赖。
- 影响：当前只能完成 Python 字节码编译检查，暂时不能执行应用导入、OpenAPI 生成和 API 测试。
- 处理：使用 Codex 工作区提供的 Python，在 `backend/.venv` 中安装 `requirements.txt` 依赖；`.venv` 已加入忽略规则。
- 验证：FastAPI 应用已成功导入并生成 `/api/v1/projects` 路由；健康检查与 OpenAPI 自动测试通过。项目 CRUD 的真实数据库测试将在 MySQL 迁移完成后补充。

## ISSUE-008：Service 方法名遮蔽 Python 内置 `list`

- 状态：已解决
- 范围：后端
- 发现时间：2026-08-27
- 现象：`BoreholeService` 已定义名为 `list` 的方法，类体后续注解 `list[BoreholeSegmentInput]` 在运行时把 `list` 解析成了该方法，导致模块导入失败。
- 影响：FastAPI 无法启动和生成 OpenAPI。
- 处理：启用 `from __future__ import annotations`，推迟注解求值，避免类命名空间遮蔽内置泛型。
- 验证：应用导入与 OpenAPI 测试恢复通过。

## ISSUE-009：旧业务数据实际位于 server 目录

- 状态：已解决
- 范围：数据
- 发现时间：2026-08-27
- 现象：早期按 `backend/data` 查找数据未找到，实际旧数据位于 `server/data`，模型位于 `server/static/models`。
- 处理：新增可配置的 `SOURCE_DATA_DIR`、`SOURCE_MODEL_DIR`，并实现幂等导入命令，统一导入项目、模型、钻孔层段和工作面。
- 验证：迁移前后项目、模型、钻孔、层段和工作面数量一致，抽样坐标与深度一致。

## ISSUE-010：Docker 可用但 MySQL 8.4 镜像拉取受阻

- 状态：已解决
- 范围：后端 / 工程化
- 发现时间：2026-08-27
- 现象：Docker Server 已启动，但 Compose 指定的 `mysql:8.4` 镜像在当前网络下无法完成拉取；本机已有 `mysql:8.0` 镜像。
- 处理：开发环境切换到兼容本项目 SQL 特性的 MySQL 8.0 镜像，继续执行真实迁移与 CRUD 校验。
- 验证：MySQL 8.0 容器已启动，两版 Alembic 迁移执行成功；真实数据已导入，并通过 FastAPI 接口完成数量和模型文件抽样。

## ISSUE-011：工作台首屏包体积偏大

- 状态：待处理
- 范围：前端 / 工程化
- 发现时间：2026-08-27
- 现象：生产构建中 `GeoWorkspace` 路由产物约 1.83 MB，gzip 后约 550 KB，Vite 发出 chunk 大小警告。
- 影响：首次进入工作台的脚本下载、解析与执行成本较高。
- 原因：Three.js、ECharts、Element Plus 组件和现有三维工具集中进入同一路由 chunk。
- 计划：工作台形态稳定后再做 ECharts Dock 延迟加载、Element Plus 按需引入，以及 Three.js examples 工具按功能动态导入，避免在结构重构阶段过早拆包。
- 验证：构建产物中主工作台 gzip 体积下降，并对首次交互时间进行前后对比。

## ISSUE-012：窄视口下旧三维工具栏超出中央画布

- 状态：已解决
- 范围：前端
- 发现时间：2026-08-27
- 现象：左右面板同时打开时，中央视口宽度小于旧工具栏内容宽度，靠左的工具按钮被裁掉。
- 处理：在工作台容器内限制工具栏最大宽度并允许横向滚动，各工具组禁止收缩。
- 验证：1280×720 工作台中全部工具可通过横向滚动访问，面板不再覆盖工具栏。

## ISSUE-013：钻孔编码存在大小写差异

- 状态：已解决
- 范围：数据迁移
- 发现时间：2026-08-28
- 现象：地层表使用 `Jb52`，坐标表使用 `JB52`，直接按原字符串连接会产生一个无坐标钻孔和一个无地层钻孔。
- 处理：匹配键统一转换为大写并去除非字母数字字符，展示编码以坐标表为准；数据库最终只保存 `JB52` 一条记录。
- 验证：导入后钻孔总数为坐标表的 210 条，不产生额外的大小写重复项。

## ISSUE-014：地层数据包含零厚度占位记录

- 状态：接受业务数据
- 范围：数据 / Three.js
- 发现时间：2026-08-28
- 现象：1,736 条地层记录中有 375 条厚度为 0，属于地层缺失或层位占位信息。
- 处理：数据库完整保留记录供统计和追溯；三维钻孔生成时跳过零厚度段，避免创建退化几何体。
- 验证：导入审计记录同时保存总层段数与零厚度层段数。

## ISSUE-015：模型元数据与静态目录不一致

- 状态：已解决
- 范围：模型资产 / 数据迁移
- 发现时间：2026-08-28
- 现象：`models_meta.json` 仅登记 `model_20251103_160811.glb`，但文件不存在；静态目录实际有 14 个 GLB。
- 处理：以静态目录递归扫描为权威来源，元数据文件只在文件名匹配时补充名称和描述；每个文件计算 SHA-256 并生成模型版本记录。
- 验证：数据库创建 14 个模型资产和 14 个当前版本，抽样模型文件接口返回 200。

## ISSUE-016：导入脚本直接运行时找不到 app 包

- 状态：已解决
- 范围：工程化 / 后端
- 发现时间：2026-08-28
- 现象：执行 `python scripts/import_server_data.py` 时，Python 仅把 `scripts` 放入模块搜索路径，无法导入同级的 `app` 包。
- 处理：脚本启动时显式加入后端根目录，并复用应用的异步 Session Factory。
- 验证：命令连续执行两次成功，业务表数量保持不变，仅新增导入审计记录。

## ISSUE-017：本地拖入模型不响应地层透明度

- 状态：已解决
- 范围：前端 / Three.js
- 发现时间：2026-08-28
- 现象：拖入的 GLB 在 `ModelManager` 中注册为 `custom`，而地层透明度只遍历 `stratum` 模型。
- 处理：将作为地质模型导入的本地 GLB 统一注册为 `stratum`，加载后立即同步当前全局透明度与边缘线状态。
- 验证：TypeScript 检查与生产构建通过；需要继续进行浏览器交互回归。

## ISSUE-018：新版地层树缺少实际材质编辑入口

- 状态：已解决
- 范围：前端 / 交互
- 发现时间：2026-08-28
- 现象：资源树只显示每个 Mesh 的颜色点，点击行为仅切换显隐，没有颜色和单层透明度控件。
- 处理：增加地层单元选中态、颜色选择器和单层透明度滑块，通过 Pinia 控制记录驱动 Three.js 材质更新；材质修改统一落到业务原始材质，避免被选中高亮的临时材质覆盖。
- 验证：TypeScript 检查与生产构建通过；需要分别抽样无纹理和带纹理 GLB。

## ISSUE-019：Vite 默认代理地址与 FastAPI 监听地址不一致

- 状态：已解决
- 范围：前后端联调
- 发现时间：2026-08-28
- 现象：Vite 自动选取局域网 IPv4 作为代理目标，但 FastAPI 默认只监听 `127.0.0.1`，可能导致项目中心误判数据服务离线。
- 处理：开发环境默认代理固定为 `http://127.0.0.1:8000`，远程环境继续通过 `VITE_BACKEND_URL` 覆盖。
- 验证：生产构建通过；开发服务器启动后需确认项目中心显示数据库项目和 14 个模型资源。

## ISSUE-020：投影坐标模型与钻孔未使用同一原点

- 状态：已解决
- 范围：坐标系统 / Three.js
- 发现时间：2026-08-28
- 现象：钻孔加载时减去了坐标中心，但真实投影坐标 GLB 保留了百万级坐标，导致两类对象无法重合显示。
- 处理：项目 Store 保存数据库中的坐标原点；检测到 X/Y 为投影坐标的模型时，同钻孔一起减去项目原点。项目垂向比例更新为 20，与旧模型生成流程一致。
- 验证：主静态模型归一化后平面范围约为 X `-6316~5860`、Y `-6032~6320`，与钻孔范围重合；生产构建通过。
- 补充：`200_output_model_gpr_rf.glb` 和 `20scale_output_model_gpr_rf.glb` 使用 X/Z 投影平面与 Y 高程，加载时自动识别并映射为统一的 X/Y 平面与 Z 高程。

## ISSUE-021：钻孔柱状图仍请求旧 API

- 状态：已解决
- 范围：前后端联动 / ECharts
- 发现时间：2026-08-28
- 现象：属性面板点击“查看柱状图”后调用旧的 `/api/boreholes/{id}`，与 FastAPI `/api/v1/boreholes/{id}` 不匹配。
- 处理：资源导入时保留列表接口已经返回的完整地层段，点击钻孔优先直接使用 Pinia 中的详情；无缓存时请求 FastAPI，旧接口仅作为兼容回退。加载成功后自动打开底部柱状图 Dock。
- 验证：类型检查和生产构建通过。

## ISSUE-022：全局透明度入口与单地层编辑重复

- 状态：已解决
- 范围：前端交互
- 发现时间：2026-08-28
- 现象：左右面板各有一个全局地层透明度滑块，同时又需要单地层材质参数，交互语义重复且会互相覆盖。
- 处理：移除左右面板的全局透明度滑块，只在地层树中选中具体 Mesh 后提供独立颜色与透明度编辑。
- 验证：生产构建通过，每个控制项以唯一 `modelId + mesh id` 定位材质。

## ISSUE-023：投影坐标使用 MySQL FLOAT 导致精度损失

- 状态：已解决
- 范围：数据库 / 坐标系统
- 发现时间：2026-08-28
- 现象：三千七百万量级的高斯投影坐标存入单精度 FLOAT 后出现米级误差，影响模型与钻孔精确套合。
- 处理：项目原点和钻孔 X/Y/Z 字段迁移为 DOUBLE，迁移后重新从 Excel 导入原始坐标。
- 验证：接口返回的项目原点恢复完整小数精度，Alembic 到达 `20260828_0003`。

## ISSUE-024：剖切辅助面控制与默认行为不符合工具语义

- 状态：已解决
- 范围：前端交互 / Three.js
- 发现时间：2026-08-28
- 现象：剖切工具额外提供辅助面显示开关，既占用工具栏空间，也会在启用剖切时遮挡地质模型。
- 处理：移除辅助面显示状态和界面开关，剖切工具统一以 `showHelper: false` 启用，底层工具的默认值也改为不显示辅助面。
- 验证：浏览器启用剖切后仅显示轴向和位置控制，场景中没有辅助平面。

## ISSUE-025：浏览器环境不支持 prompt 导致标注失败

- 状态：已解决
- 范围：前端交互 / Three.js
- 发现时间：2026-08-28
- 现象：标注工具点击模型后调用 `window.prompt` 获取文字，在嵌入式浏览器中抛出 `prompt() is not supported`，标注无法创建。
- 处理：将拾取位置与文本输入解耦；Three.js 工具只回传三维坐标，Vue 在场景内展示非模态标注编辑器，确认后再调用工具创建标注。
- 验证：浏览器点击模型、输入“测试标注”并确认后，编辑器关闭且标注成功保留，控制台没有新增错误。

## ISSUE-026：钻孔柱状图横向过宽且底部被裁切

- 状态：已解决
- 范围：前端布局 / ECharts
- 发现时间：2026-08-28
- 现象：柱状图直接占满底部 Dock，横向阅读距离过长；Dock 高度不足时图表底部刻度被截断，容器尺寸变化后也不能稳定重算。
- 处理：将钻孔分析区拆为摘要栏和最大宽度 520px 的图表区，底部 Dock 调整为 280px，并使用 `ResizeObserver` 驱动 ECharts 自适应。
- 验证：宽屏浏览器打开钻孔柱状图后，图表保持紧凑宽度，0m 至底部刻度完整显示。

## ISSUE-027：钻孔分层在属性侧栏中以原始 JSON 显示

- 状态：已解决
- 范围：前端信息架构
- 发现时间：2026-08-28
- 现象：`layers` 数组沿用通用键值渲染，被直接转换成长 JSON 字符串，层名和深度区间难以查看。
- 处理：为钻孔属性增加专用结构化视图，分别展示深度指标、投影坐标和可滚动分层列表；每层显示颜色标识、层名、顶底深度及厚度。
- 验证：浏览器选中钻孔后，侧栏显示专用指标卡和分层区域，不再输出 `layers` 原始 JSON。
